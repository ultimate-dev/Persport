const NAME = "ReactFullApp";
const CURSOR = {
  innerSize: 8,
  outerSize: 40,
  color: "255, 255 ,255",
  outerAlpha: 0.2,
  innerScale: 0.6,
  outerScale: 1.2,
};

export default {
  NAME,
  CURSOR,
};
